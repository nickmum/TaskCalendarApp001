<Window x:Class="TaskCalendarApp001.Views.NewTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Task" Height="300" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="New Task" FontSize="20" Margin="0,0,0,10"/>

        <!-- Future: Add form fields for task creation -->
        <!-- Future: Add validation -->
        <!-- Future: Add recurrence options -->

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="75" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Create" Width="75" Click="Create_Click"/>
        </StackPanel>
    </Grid>
</Window>